apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: blue-green
  name: blue-green
spec:
  port:
    targetPort: 8080
  to:
  alternateBackends:
  - kind: Service
    name: user{n}-hello-world
    weight: 10